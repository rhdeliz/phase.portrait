% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_values.R
\name{normalize_values}
\alias{normalize_values}
\title{Normalize Values}
\usage{
normalize_values(df, normalization = "max", save = FALSE)
}
\arguments{
\item{df}{A data frame with a \code{value} column to be normalized.}

\item{normalization}{A character string specifying the normalization method.
Options are:
\itemize{
\item \code{"max"} (default): Scales \code{value} by dividing by the maximum absolute value.
\item \code{"min-max"}: Scales \code{value} to a 0-1 range by subtracting the minimum and dividing by the range.
}}

\item{save}{Logical, if TRUE, saves the output as a compressed CSV file. Default is FALSE.}
}
\value{
A data frame with the normalized \code{value} column.
}
\description{
This function normalizes the \code{value} column in a data frame using either max or min-max normalization.
It scales the values based on the specified method and optionally saves the output as a compressed CSV file.
}
\examples{
# Create an example data frame for normalization
df <- data.frame(
  value = c(1, 2, 3, 4, 5),
  variable = "var1",
  pseudotime = 1:5
)

# Apply min-max normalization
normalized_df <- normalize_values(df, normalization = "min-max")

# Apply max normalization and save the output as a CSV file
normalized_df <- normalize_values(df, normalization = "max", save = TRUE)
}
